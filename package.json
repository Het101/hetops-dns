{
  "name": "hetops-dns",
  "version": "5.0.0",
  "description": "HetOps DNS Intelligence — SSL Labs replacement with full TLS analysis, vulnerability detection, certificate chain validation, and comprehensive security auditing",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "lint": "node -c server.js",
    "test": "echo \"No tests configured\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "express-rate-limit": "^8.3.2",
    "whois": "^2.16.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
